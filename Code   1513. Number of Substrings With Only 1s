Input: s = "0110111"
Output: 9
Explanation: There are 9 substring in total with only 1's characters.
"1" -> 5 times.
"11" -> 3 times.
"111" -> 1 time.
Example 2:

Input: s = "101"
Output: 2
Explanation: Substring "1" is shown 2 times in s.


//---Code---//

class Solution {
    public static long backSum(long num) {
        if(num == 1) {return num;}
        long sum = 0;
        for(long i = num;i >= 0;i--) {
            sum += i;
        }
        return sum;
    }
    public int numSub(String s) {
        String str = s + "0";
        ArrayList<Long> list = new ArrayList<>();
        long counter = 0;
        for(char ch : str.toCharArray()) {
            if(ch == '1') {
                counter++;
            }else{
                if(counter > 0) {
                    list.add(counter);
                    counter = 0;
                }
            }
        }
        long sum = 0;
        long mod = 1000000007;
        for(long num : list) {
            long temp = backSum(num);
            sum = (sum + temp) % mod;
        }
        return (int)sum;
    }
}

